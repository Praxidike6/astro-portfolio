<section
  id='contact'
  class='contact'>
  <div class='contact-inner'>
    <div class='section-heading'>
      <p class='section-number'>05</p>
    </div>

    <div class='contact-content'>
      <div class='contact-main'>
        <h2>
          Let's build<br />
          something<br />
          interesting.
        </h2>

        <p>
          Have a project, idea or problem you'd like to discuss? I'd love to
          hear about it.
        </p>

        <a
          href='mailto:mcdonnell.stuart@gmail.com'
          class='contact-button'>
          Get in touch →
        </a>
      </div>

      <div class='contact-links'>
        <a
          href='https://www.linkedin.com/in/stuart-mcdonnell-762955166'
          target='_blank'
          rel='noopener noreferrer'
          aria-label='LinkedIn profile'>
          LinkedIn
          <span>↗</span>
        </a>

        <a
          href='mailto:mcdonnell.stuart@gmail.com'
          aria-label='Email Stuart McDonnell'>
          Email
          <span>↗</span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    position: relative;
    isolation: isolate;

    min-height: calc(100dvh - 80px);

    padding: 0 var(--page-padding) 60px;

    overflow: clip;
  }

  .contact::before,
  .contact::after {
    z-index: 0;
  }

  .contact::before {
    content: '';

    position: absolute;

    inset: 10% 5%;

    background:
      radial-gradient(circle at 20% 50%, rgb(91 206 250 / 8%), transparent 45%),
      radial-gradient(circle at 80% 40%, rgb(74 144 226 / 6%), transparent 40%);

    filter: none;

    pointer-events: none;
  }

  .contact::after {
    content: '';

    position: absolute;

    inset: 0;

    background-image:
      linear-gradient(rgb(91 206 250 / 3%) 1px, transparent 1px),
      linear-gradient(90deg, rgb(91 206 250 / 3%) 1px, transparent 1px);

    background-size: 40px 40px;

    opacity: 0.4;

    pointer-events: none;
  }

  .contact-inner {
    position: relative;
    z-index: 1;

    width: 100%;
    max-width: var(--content-width);

    min-height: calc(100dvh - 140px);

    margin: 0 auto;

    display: grid;
    grid-template-rows: auto 1fr;
  }

  .section-heading {
    margin-bottom: 35px;
  }

  .section-number {
    margin: 0;

    color: var(--color-cyan);

    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: 100px;

    align-items: center;
    align-self: center;
  }

  .contact-main h2 {
    margin: 0;

    font-size: clamp(3.5rem, 8vw, 8rem);
    line-height: 0.88;
    letter-spacing: -0.06em;
  }

  .contact-main > p {
    max-width: 500px;

    margin: 40px 0 30px;

    color: #9ba7b5;

    font-size: 1.1rem;
    line-height: 1.8;
  }

  .contact-button {
    display: inline-block;

    padding: 14px 24px;

    border: 1px solid var(--color-cyan);
    border-radius: 4px;

    color: var(--color-cyan);

    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;

    transition:
      background 0.25s ease,
      color 0.25s ease,
      transform 0.25s ease,
      box-shadow 0.25s ease;
  }

  .contact-button:hover {
    background: var(--color-cyan);
    color: var(--color-background);

    transform: translateY(-2px);

    box-shadow:
      0 0 15px rgb(91 206 250 / 35%),
      0 0 30px rgb(91 206 250 / 15%);
  }

  .contact-links {
    display: flex;
    flex-direction: column;

    border-top: 1px solid rgb(91 206 250 / 15%);
  }

  .contact-links a {
    display: flex;
    justify-content: space-between;
    align-items: center;

    padding: 18px 0;

    border-bottom: 1px solid rgb(91 206 250 / 15%);

    color: #9ba7b5;

    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;

    transition:
      color 0.25s ease,
      padding 0.25s ease;
  }

  .contact-links a:hover {
    padding-left: 8px;
    color: var(--color-cyan);
  }

  .contact-links span {
    font-size: 1rem;
  }

  @media (max-width: 1000px) {
    .contact {
      min-height: calc(100dvh - 80px);
      padding: 0 var(--page-padding) 50px;
    }

    .contact-inner {
      min-height: calc(100dvh - 130px);
    }

    .section-heading {
      margin-bottom: 30px;
    }
  }

  @media (max-width: 800px) {
    .contact-content {
      grid-template-columns: 1fr;
      gap: 60px;
    }

    .contact-main h2 {
      font-size: clamp(3rem, 15vw, 5rem);
    }
  }
</style>
