<section
  id='contact'
  class='contact'>
  <div class='contact-inner'>
    <div class='section-heading'>
      <p class='section-number'>05</p>
    </div>

    <div class='contact-content'>
      <div class='contact-main'>
        <h2>
          Let's build<br />
          something<br />
          useful.
        </h2>

        <p>
          Have a project, idea or technical challenge you'd like to discuss? I
          work across web development, design and practical technology
          solutions, and I’d be happy to talk through what you need.
        </p>

        <a
          href='mailto:mcdonnell.stuart@gmail.com'
          class='contact-button'>
          Get in touch →
        </a>
      </div>

      <div class='contact-links'>
        <a
          href='https://www.linkedin.com/in/stuart-mcdonnell-762955166'
          target='_blank'
          rel='noopener noreferrer'
          aria-label='LinkedIn profile'>
          LinkedIn
          <span>↗</span>
        </a>

        <a
          href='mailto:mcdonnell.stuart@gmail.com'
          aria-label='Email Stuart McDonnell'>
          Email
          <span>↗</span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    position: relative;
    isolation: isolate;

    min-height: calc(100dvh - 80px);

    padding: 0 var(--page-padding) 60px;
  }

  .contact::before,
  .contact::after {
    z-index: 0;
  }

  .contact::before {
    content: '';

    position: absolute;
    inset: 10% 5%;

    background:
      radial-gradient(circle at 20% 50%, rgb(91 206 250 / 8%), transparent 45%),
      radial-gradient(circle at 80% 40%, rgb(74 144 226 / 6%), transparent 40%);

    pointer-events: none;
  }

  .contact::after {
    content: '';

    position: absolute;
    inset: 0;

    background-image:
      linear-gradient(rgb(91 206 250 / 3%) 1px, transparent 1px),
      linear-gradient(90deg, rgb(91 206 250 / 3%) 1px, transparent 1px);

    background-size: 40px 40px;

    opacity: 0.4;

    pointer-events: none;
  }

  .contact-inner {
    position: relative;
    z-index: 1;

    width: 100%;
    max-width: var(--content-width);
    min-height: calc(100dvh - 140px);

    margin: 0 auto;

    display: grid;
    grid-template-rows: auto 1fr;
  }

  .section-heading {
    margin-bottom: 35px;
  }

  .section-number {
    margin: 0;

    color: var(--color-cyan);

    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 300px;

    gap: 100px;

    align-items: center;
    align-self: center;
  }

  .contact-main h2 {
    margin: 0;

    font-size: clamp(3.5rem, 8vw, 8rem);
    line-height: 0.88;
    letter-spacing: -0.06em;
  }

  .contact-main > p {
    max-width: 500px;

    margin: 40px 0 30px;

    color: #9ba7b5;

    font-size: 1.1rem;
    line-height: 1.8;
  }

  .contact-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;

    padding: 14px 24px;

    border: 1px solid var(--color-cyan);
    border-radius: 4px;

    color: var(--color-cyan);

    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;

    transition:
      background 0.25s ease,
      color 0.25s ease,
      transform 0.25s ease,
      box-shadow 0.25s ease;
  }

  .contact-button:hover {
    background: var(--color-cyan);
    color: var(--color-background);

    transform: translateY(-2px);

    box-shadow:
      0 0 15px rgb(91 206 250 / 35%),
      0 0 30px rgb(91 206 250 / 15%);
  }

  .contact-links {
    display: flex;
    flex-direction: column;

    border-top: 1px solid rgb(91 206 250 / 15%);
  }

  .contact-links a {
    display: flex;
    justify-content: space-between;
    align-items: center;

    min-height: 56px;

    padding: 18px 0;

    border-bottom: 1px solid rgb(91 206 250 / 15%);

    color: #9ba7b5;

    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;

    transition:
      color 0.25s ease,
      padding 0.25s ease;
  }

  .contact-links a:hover {
    padding-left: 8px;

    color: var(--color-cyan);
  }

  .contact-links span {
    font-size: 1rem;
  }

  /* --------------------------------
     Tablet
  -------------------------------- */

  @media (max-width: 1000px) and (min-width: 801px) {
    .contact {
      min-height: calc(100dvh - 80px);

      padding: 0 var(--page-padding) 60px;
    }

    .contact-inner {
      min-height: auto;

      display: block;
    }

    .section-heading {
      margin-bottom: 36px;
    }

    .contact-content {
      grid-template-columns:
        minmax(0, 1fr)
        240px;

      gap: 50px;

      align-items: end;
      align-self: auto;
    }

    .contact-main h2 {
      font-size: clamp(3.8rem, 8vw, 5rem);
    }
  }

  /* --------------------------------
     Mobile
  -------------------------------- */

  @media (max-width: 800px) {
    .contact {
      min-height: auto;

      padding: 80px var(--page-padding) 70px;
    }

    .contact-inner {
      min-height: auto;

      display: block;
    }

    .section-heading {
      margin-bottom: 32px;
    }

    .contact-content {
      grid-template-columns: 1fr;

      gap: 50px;

      align-items: start;
    }

    .contact-main h2 {
      font-size: clamp(3rem, 15vw, 5rem);
    }

    .contact-main > p {
      max-width: 600px;

      margin-top: 30px;

      font-size: 1rem;
      line-height: 1.75;
    }

    .contact-links {
      max-width: 600px;
    }
  }

  @media (max-width: 560px) {
    .contact {
      padding: 70px var(--page-padding) 55px;
    }

    .section-heading {
      margin-bottom: 26px;
    }

    .contact-content {
      gap: 42px;
    }

    .contact-main h2 {
      font-size: clamp(2.8rem, 14vw, 4.2rem);
      line-height: 0.92;
    }

    .contact-main > p {
      margin: 26px 0 24px;

      font-size: 0.95rem;
      line-height: 1.75;
    }

    .contact-button {
      width: 100%;

      padding: 14px 18px;
    }

    .contact-links a {
      min-height: 60px;

      padding: 18px 2px;
    }

    .contact-links a:hover {
      padding-left: 6px;
    }
  }

  /* --------------------------------
     Reduced motion
  -------------------------------- */

  @media (prefers-reduced-motion: reduce) {
    .contact-button,
    .contact-links a {
      transition: none;
    }

    .contact-button:hover {
      transform: none;
    }
  }
</style>
