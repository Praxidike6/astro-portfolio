<section
  id='contact'
  class='contact'>
  <div class='contact-inner'>
    <div class='section-heading'>
      <p class='section-number'>06</p>
    </div>

    <div class='contact-content'>
      <div class='contact-main'>
        <h2>
          Let's build<br />
          something<br />
          interesting.
        </h2>

        <p>
          Have a project, idea or problem you'd like to discuss? I'd love to
          hear about it.
        </p>

        <a
          href='mailto:your@email.com'
          class='contact-button'>
          Get in touch →
        </a>
      </div>

      <div class='contact-links'>
        <a
          href='#'
          aria-label='GitHub'>
          GitHub
          <span>↗</span>
        </a>

        <a
          href='#'
          aria-label='LinkedIn'>
          LinkedIn
          <span>↗</span>
        </a>

        <a
          href='mailto:your@email.com'
          aria-label='Email'>
          Email
          <span>↗</span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    min-height: 80vh;

    padding: 140px var(--page-padding) 100px;

    display: flex;
    align-items: center;
  }

  .contact-inner {
    width: 100%;
    max-width: var(--content-width);
    margin: 0 auto;
  }

  .section-heading {
    margin-bottom: 70px;
  }

  .section-number {
    margin: 0;

    color: var(--color-cyan);

    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: 100px;
    align-items: end;
  }

  .contact-main h2 {
    margin: 0;

    font-size: clamp(3.5rem, 8vw, 8rem);
    line-height: 0.88;
    letter-spacing: -0.06em;
  }

  .contact-main > p {
    max-width: 500px;

    margin: 40px 0 30px;

    color: #9ba7b5;

    font-size: 1.1rem;
    line-height: 1.8;
  }

  .contact-button {
    display: inline-block;

    padding: 14px 24px;

    border: 1px solid var(--color-cyan);
    border-radius: 4px;

    color: var(--color-cyan);

    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;

    transition:
      background 0.25s ease,
      color 0.25s ease,
      transform 0.25s ease;
  }

  .contact-button:hover {
    background: var(--color-cyan);
    color: var(--color-background);
    transform: translateY(-2px);
  }

  .contact-links {
    display: flex;
    flex-direction: column;

    border-top: 1px solid rgb(91 206 250 / 15%);
  }

  .contact-links a {
    display: flex;
    justify-content: space-between;
    align-items: center;

    padding: 18px 0;

    border-bottom: 1px solid rgb(91 206 250 / 15%);

    color: #9ba7b5;

    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;

    transition:
      color 0.25s ease,
      padding 0.25s ease;
  }

  .contact-links a:hover {
    padding-left: 8px;
    color: var(--color-cyan);
  }

  .contact-links span {
    font-size: 1rem;
  }

  @media (max-width: 800px) {
    .contact {
      min-height: auto;
      padding: 100px var(--page-padding);
    }

    .contact-content {
      grid-template-columns: 1fr;
      gap: 60px;
    }

    .contact-main h2 {
      font-size: clamp(3rem, 15vw, 5rem);
    }
  }
</style>
