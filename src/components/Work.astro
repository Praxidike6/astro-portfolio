<section
  id='work'
  class='work'>
  <div class='work-inner'>
    <div class='section-heading'>
      <p class='section-number'>03</p>
      <h2>Work</h2>
    </div>

    <div class='projects'>
      <article class='project'>
        <p class='project-number'>01</p>

        <div class='project-content'>
          <h3>Project Name</h3>

          <p class='project-description'>
            A short description of the project, the problem it solved and the
            technology used to build it.
          </p>

          <div class='project-tags'>
            <span>Astro</span>
            <span>JavaScript</span>
            <span>API</span>
          </div>

          <a
            href='#'
            class='project-link'>
            View project →
          </a>
        </div>
      </article>

      <article class='project'>
        <p class='project-number'>02</p>

        <div class='project-content'>
          <h3>Project Name</h3>

          <p class='project-description'>
            A short description of the project, the problem it solved and the
            technology used to build it.
          </p>

          <div class='project-tags'>
            <span>HTML</span>
            <span>CSS</span>
            <span>JavaScript</span>
          </div>

          <a
            href='#'
            class='project-link'>
            View project →
          </a>
        </div>
      </article>
    </div>
  </div>
</section>

<style>
  .work {
    padding: 140px var(--page-padding);
  }

  .work-inner {
    max-width: var(--content-width);
    margin: 0 auto;
  }

  .section-heading {
    display: flex;
    align-items: baseline;
    gap: 20px;
    margin-bottom: 70px;
  }

  .section-number {
    color: var(--color-cyan);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  h2 {
    margin: 0;
    font-size: clamp(2.5rem, 5vw, 5rem);
    line-height: 1;
    letter-spacing: -0.04em;
  }

  .projects {
    display: grid;
    gap: 30px;
  }

  .project {
    display: grid;
    grid-template-columns: 80px 1fr;

    padding: 50px;

    border: 1px solid rgb(91 206 250 / 12%);
    background: rgb(255 255 255 / 2%);

    transition:
      border-color 0.3s ease,
      transform 0.3s ease,
      background 0.3s ease;
  }

  .project:hover {
    border-color: rgb(91 206 250 / 35%);
    background: rgb(91 206 250 / 3%);
    transform: translateY(-4px);
  }

  .project-number {
    margin: 0;

    color: var(--color-cyan);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.1em;
  }

  .project-content h3 {
    margin: 0 0 20px;

    font-size: clamp(1.8rem, 3vw, 2.8rem);
    letter-spacing: -0.03em;
  }

  .project-description {
    max-width: 650px;

    margin: 0 0 25px;

    color: #9ba7b5;
    line-height: 1.8;
  }

  .project-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;

    margin-bottom: 30px;
  }

  .project-tags span {
    padding: 6px 10px;

    border: 1px solid rgb(91 206 250 / 20%);
    border-radius: 3px;

    color: var(--color-cyan);
    font-size: 0.7rem;
    letter-spacing: 0.06em;
  }

  .project-link {
    color: var(--color-cyan);

    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  @media (max-width: 800px) {
    .work {
      padding: 100px var(--page-padding);
    }

    .project {
      grid-template-columns: 1fr;
      gap: 25px;
      padding: 30px;
    }
  }
</style>
